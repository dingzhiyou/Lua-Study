lua_yieldk返回的参数是没有范围的，并且这种返回方式下lua_resume返回后的gettop也是完全按照返回来显示，没有正确性
lua_yield只能在函数中直接返回，后面的代码都执行不到了如果要继续执行，使用lua_yieldk添加延续函数，没有别的用法
通过lua_yield，lua_yieldk返回的返回值数量，在lua_resume函数中都可以无检测的直接拿到这个数值，但是实际是不是不检测
但是通过lua_resume上传的，除了第一次调用，也是就别人yield之后，你在去上传的，这时候无论你的参数是多少个，在对端函数得到的就会是正常的数量
正确的做法是每次yield后都进行栈的清理工作，重新向栈添加值后在进行调用